import { TaskCommandWidget } from '@/widgets/TaskCommand';

export default function TaskCommandPage() {
 return (
 <div className="h-full flex flex-col p-4 bg-slate-50">
 <TaskCommandWidget />
 </div>
 );
}
